name: mobile_app
description: "Aplicación de reportes ciudadanos para Piura."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Paquetes de Comunicación y Datos
  http: ^1.2.1
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  socket_io_client: ^3.1.2
  jwt_decoder: ^2.0.1

  # Paquetes Geoespaciales
  flutter_map: ^8.0.0
  latlong2: ^0.9.1
  geolocator: ^14.0.0
  permission_handler: ^12.0.1
  flutter_map_marker_cluster: ^8.0.0

  # Paquetes de UI y Utilidades
  image_picker: ^1.1.2
  intl: ^0.20.2
  shimmer: ^3.0.0
  fl_chart: ^1.1.1

  # Paquetes de Funcionalidades Nativas
  flutter_local_notifications: ^19.0.0
  flutter_background_service: ^5.0.1
  pdf: ^3.11.3
  path_provider: ^2.1.5
  open_file: ^3.5.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/